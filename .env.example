# Required for OpenAI Realtime collaboration. Keep this server-side only.
OPENAI_API_KEY=

# Defaults to 3001. Custom ports are for standalone server/direct-client use;
# npm run dev requires 3001 because Vite's API proxy target is fixed.
PORT=3001
